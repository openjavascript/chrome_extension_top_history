const config = {
    "name": "config.js"
    , "dbName": "top_history"
    , "dbDesc": "top_history database"
    , "dbDataTableName": "notes"

    , "operationDelayMs": 500
};

export default config;

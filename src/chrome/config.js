const config = {
    "name": "config.js"
    , "dbName": "top-history"
    , "dbDesc": "top-history database"
    , "dbDataTableName": "notes"

    , "operationDelayMs": 500
};

export default config;
